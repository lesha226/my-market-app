<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="create-orders" author="lesha226@yandex.ru">
        <preConditions>
            <not>
                <tableExists tableName="orders"/>
            </not>
        </preConditions>
        <rollback>
            <dropTable tableName="orders"/>
        </rollback>
        <!--
            create table if not exists orders (
                id  bigint  generated by default as identity,
                constraint pk_orders primary key (id)
            );
        -->
        <createTable tableName="orders">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKeyName="pk_orders" primaryKey="true" nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <changeSet id="create-order_items" author="lesha226@yandex.ru">
        <preConditions>
            <not>
                <tableExists tableName="order_items"/>
            </not>
        </preConditions>
        <rollback>
            <dropTable tableName="order_items"/>
        </rollback>
        <!--
            create table if not exists order_items (
                id          bigint          generated by default as identity,
                order_id    bigint          not null,
                item_id     bigint          not null,
                count       integer         not null,
                constraint pk_order_items primary key (id),
                constraint uk_order_items unique (order_id, item_id),
                constraint fk_order_items_item_id foreign key (item_id) references items(id),
                constraint fk_order_items_order_id foreign key (order_id) references orders(id)
            );
        -->
        <createTable tableName="order_items">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" primaryKeyName="pk_order_items" nullable="false"/>
            </column>
            <column name="order_id" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="item_id" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="count" type="integer">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addUniqueConstraint tableName="order_items"
                             columnNames="order_id, item_id"
                             constraintName="uk_order_items"/>

        <addForeignKeyConstraint baseTableName="order_items"
                                 baseColumnNames="item_id"
                                 constraintName="fk_order_items_item_id"
                                 referencedTableName="items"
                                 referencedColumnNames="id"/>

        <addForeignKeyConstraint baseTableName="order_items"
                                 baseColumnNames="order_id"
                                 constraintName="fk_order_items_order_id"
                                 referencedTableName="orders"
                                 referencedColumnNames="id"/>
    </changeSet>

</databaseChangeLog>